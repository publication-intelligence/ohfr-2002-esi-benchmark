# Corrections found during V10 execution

All four candidates completed the supported initial V10 migration against release `ohfr-2002-successor-v10-2026-09-17`. The [migration-binding check](../validation/reconciliation/four-candidate-v10-initial-migration-binding-verification.v1.json) verifies their exact common source proof, lock and semantic policy. Fresh factual review continues. Final native results and website import await the following reviewed corrections; no new comparative scores are asserted.

## Inspected semantic uncertainty

The installed `815bcb66` runtime cannot truthfully serialize a physically inspected, scope-known locator whose semantic interpretation remains unresolved. Its neutral locator status requires unavailable treatment and unavailable/ambiguous scope; low confidence alone does not neutralize numeric credit. Evaluators preserve the actual known and unknown axes instead of manufacturing certainty or unavailable evidence.

The contract owner's [v1 interpretation](v10-semantic-uncertainty-addendum-v1.md) and controlling [v2 clarification](v10-semantic-uncertainty-addendum-v2.md) specify the correction. Known axes remain factual, unknown axes receive neutral bounds, selectivity applicability uses only jointly consistent existing treatment states, and semantic assessment insufficiency remains separate from physical validity. Numeric invariance does not restore evaluation authority. Existing confirmed gate facts and outcome precedence remain intact. The reviewed scope changes no score mapping, weight, cap threshold, source scope or historical artifact.

The methodology owner is implementing the correction in isolation. New native schemas, fixtures, consumer compatibility, explicit execution provenance and a reviewed installation must precede adoption. The active installation remains the preserved `815bcb66` payload until that coordinated cutover.

## Identical retained distinction

The source serializes retained distinction `C005-S004` twice under `SUBJ-SUCC-2056A5304D29`; their complete values are identical. Independent source review confirms one unweighted obligation. The approved implementation may canonicalize identical retained-distinction occurrences only in the derived review inventory, retaining occurrence provenance and rejecting conflicting values. The frozen source is unchanged. Mechanical reconstruction finds 2,732 distinct parent-qualified requirements and exactly one repeated occurrence; its complete-value hash is `e215033a789fbafa5ad2d3c31c6f0449d991780c7a79707aab3a162178f1b330`.

## Unsupported relationship dependency

The [post-freeze source-only report](../successor/independent-review/post-freeze/artois-authorization-and-duplicate-distinction.referral.v1.json) confirms a missed dependency correction. The parent evidence on page 305 correctly limits Artois to princely-network context, but `REL-SUCC-8AD7BFC51F21` still asserts his individual authorization of La Rouërie's organization. No sealed relationship clarification resolves that contradiction.

The proposed minimal correction retires that edge, taking relationships from 501 to 500, while preserving both subjects, their evidence, the related comparative task, all priorities and every other source requirement. It requires a separately reviewed, versioned source freeze and new dependent access/release bindings. The initial source freeze, amendment, lock, descriptor and evaluation history remain immutable. Only this disputed relational obligation is pending; unaffected candidate review continues and its evidence is preserved for explicit compatibility review under the corrected release.

## Merge and publication status

Benchmark PR17 is back in draft while the correction is reviewed. Its earlier exact release and execution-record commits passed CI. Ordinary merge was blocked by required GitHub PR approval; automatic approval review rejected the attempted admin override because ordinary merge authorization did not cover bypassing that review control. Explicit user permission was requested and no override was performed. This merge dependency does not authorize a workaround or stop the already authorized factual evaluation work. Website deployment remains outside scope.
