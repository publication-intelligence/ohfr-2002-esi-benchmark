# Study benchmark reconciliation

No final study benchmark is selected. Legacy v3 is the incumbent because the original repository contract mandated it before a later independently reviewed native V8 release existed. That historical instruction cannot establish which release better represents the source.

The completed [source-only comparison](source-benchmark-comparison.md) recommends a **versioned, independently reviewed successor**. A separate fresh source-only check confirmed decisive defects in both unchanged releases. The comparison covered 128 of 425 pages and 78 semantic groups; every unreviewed entity remains explicit. It establishes the need for reconciliation, not complete editorial approval or a study-wide freeze. Full source-only synthesis has now begun under that recommendation, with a distinct independent review required afterward. Native organizing units are only a provisional drafting scaffold, with justified refinements from legacy; neither old release is adopted as the winner.

The two releases cover the same supplied PDF, page map, and 17 source chunks. Their semantic contents differ. Existing four-candidate aggregate scores cannot be used as a controlled comparison until one common benchmark, policy, audit mode, rubric, calculation profile, source scope, and exact chunk density basis are bound across the study.

## Source-only decision

A fresh context started on 2026-09-16 with no inherited conversation and an explicit source-only input allowlist. The coordinator has prior candidate-result exposure and will not attest candidate blindness or substitute its own editorial approval. The reviewer may delegate disjoint source chunks to equally isolated contexts. Each reviewer must attest its inputs and actual coverage.

The review compares both releases symmetrically for source coverage and omissions; subject meaning, boundaries, granularity and redundancy; priorities; evidence and treatments; acceptable access; reader tasks; relationships; uncertainties; review completeness; and editorial defects. It must cite source pages and stable entity IDs, identify source-grounded improvements in either direction, and state limitations.

The three possible recommendations are retaining v3, adopting native v2 study-wide, or creating a versioned successor with explicit synthesis decisions and independent review. Record counts, release dates, prior candidate scores, the historical mandate, and a blind union are not selection criteria. A crosswalk must distinguish equivalent, narrower, broader, split, merge, changed, unmapped and ambiguous requirements. Label/page matches are seeds only; unreviewed cases remain explicit.

The exact source PDF hash is `5f89aa2592218983c594278bfd86cc1e4b74be1dd6dd8aac5c2610a48fa34047`; page-map identity is `452602deebdae19f8e35c589f2ff2a7a0b9fc955d268b14f760391b0043e9653`; chunk-manifest identity is `5fc5450386114fdeb19838f54d3f1662c3d6dec81d4729c73f4f7ec0cb341a6f`. The 425 supplied main-text pages are the scope; absent frontmatter and endnotes remain excluded. Restricted source bytes and extracted text stay outside Git.

Legacy v3 remains frozen at commit `98dbffd0ca171b5b7db76dbe1b2b5d5265ccacab`, canonical identity `b925797fcab50b2008ad5974590e323f772e5ea7013efa84ce7606007439aeb3`. Native v2 remains frozen at canonical identity `558980b374ca4b162b12f2726e237319b170f9e8b7014f71ad7c2171e9e98983`. Native v2 is not the defective historical legacy v2. Preserve both freezes and their separate independent review histories.

## Execution hold and reuse boundary

All four candidate evaluation PRs remain in draft. No reconciliation step has rebound or rescored a candidate, activated a saved-project result, merged an evaluation PR, reset `candidate_seen`, or deployed a website. Existing V8.2 migrations and private handoffs remain historical checkpoints.

The methodology owner is implementing generic study-lock, candidate-visible atomic policy/benchmark migration, and comparison-preflight support. It must reject mixed semantic benchmarks despite a common source/methodology label, permit verified equivalent wrappers with approved lineage, require the exact shared density map, and display benchmark identity beside methodology. It does not select the benchmark.

Methodology [PR #51](https://github.com/publication-intelligence/evaluate-subject-index/pull/51) reached a tested, CI-passing checkpoint at `9335ef07c309cfcd0b94e428288e5e98f1ad7c3b`. Coordinator review then identified two remaining integration issues: current-schema successor freezes need their own valid review/registration path, and new public lineage metadata must exclude private absolute transport paths. Those corrections are in progress; the PR is not approved for installation yet. The installed runtime remains the verified c11c6cc checkpoint below.

Old missing-access ledgers cannot receive replacement hashes or IDs. Reuse requires evidence that answers the same source requirement. Independent delivered-locator and physical-reference evidence may remain useful after exact binding checks; new or changed access, treatment, task, relationship and structure obligations need substantive review and downstream recalculation. A benchmark change does not carry the score-invariance expectation of a policy-only V8.2 migration.

A successor requires a new version, a source-grounded change ledger, complete independent candidate-blind review and a supported freeze. No old freeze may be silently mutated. Selection and retrospective migration must record actual visibility and dates. Any eventual integrated website bundle must be prepared and verified before explicit publish approval; no deployment is currently authorized.

## Methodology checkpoint

The installed evaluate-subject-index skill was verified against all 111 recorded file hashes and the corresponding Git blobs at `c11c6ccb16000fe79646af16b7be01f6cbeeac78`. Its V8.2 direct gates require confirmed unsupported/no-fit delivered locators or confirmed delivered cross-references with no valid destination. Partial fit is not total wrongness.

This document records reconciliation status only; it is not a replacement evaluation-state manifest or a benchmark approval.
